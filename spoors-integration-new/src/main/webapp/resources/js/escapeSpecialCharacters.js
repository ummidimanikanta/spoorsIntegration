function escapeSpclCharacters(){
	
		/*$('input:text').each(function () {
				var temp = '';
				var value = $(this).val();
				var id = $(this).attr('id');
				
				
				if(value != null && value != '')
					value = value.replace(/([<>/])+/g, '');
				
				temp = value;
	
				if(temp != null && temp != '')
					temp = temp.replace(/([<>/ ])+/g, '');
	
				
					$(this).val($ESAPI.encoder().encodeForHTML(value));
					
		  });
    		
    		$('textarea').each(function () {
    			var temp = '';
    			var value = $(this).val();
    			
    			if(value != null && value != '')
    				value = value.replace(/([<>/])+/g, '');
    			
    			temp = value;

    			if(temp != null && temp != '')
    				temp = temp.replace(/([<>/ ])+/g, '');

    			$(this).val($ESAPI.encoder().encodeForHTML(value));
    	  });*/
    		
    	
}
